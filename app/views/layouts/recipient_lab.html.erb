<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= t("prototype.recipient_lab.title") %> — <%= t("brand.name") %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "recipient_lab", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="recipient-lab-page">
    <%= yield %>
  </body>
</html>
