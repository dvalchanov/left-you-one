<main class="recipient-empty-state" style="--empty-art: url('<%= asset_path("prototype/receiver/paper-world.webp") %>')">
  <section>
    <p><%= t("brand.name") %></p>
    <h1><%= t("public_gift.already_claimed") %></h1>
    <p><%= t("public_gift.already_claimed_context") %></p>
    <%= link_to t("public_gift.return"), start_path, class: "recipient-stage-link" %>
  </section>
</main>
